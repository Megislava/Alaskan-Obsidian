- the package includes programs such as: lockd, statd, showmount, nfsstat, gssd, idmapd, mount.nfs
- most interesting are:
	- `showmount`
		- `-e [IP]`: lists the NFS shares on the IP
	- `mount.nfs`